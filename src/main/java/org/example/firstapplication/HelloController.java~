package org.example.firstapplication;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class HelloContrainer {
    @GetMapping("/hello")
    public String hello() {
        return "Hello WOrld";
    }
}
